$(function() {
    // components for js; //=
});

// const whoWe = document.querySelectorAll('.who__line__one');
// const maxTranslate = 190; // Максимальное значение translateX

// window.addEventListener("scroll", () => {
//   const scrollPosition = window.scrollY; // Текущая позиция прокрутки

//   whoWe.forEach(whoWeItems => {
//     // Вычисляем прогресс анимации от 0 до 1 на основе текущей позиции прокрутки
//     const animationProgress = Math.min(scrollPosition / maxTranslate, 1);

//     // Применяем интерполяцию для вычисления значения transform
//     const translateValue = animationProgress * maxTranslate;
//     whoWeItems.style.transform = `translateX(${translateValue}px)`;
//   });
// });